export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  // Helio: home 路由修改为完整路由 `/dashboard/analysis/index`
  BASE_HOME = '/dashboard/analysis/index',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
export const PageWrapperFixedHeightKey = 'PageWrapperFixedHeight';
