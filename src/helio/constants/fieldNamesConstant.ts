/*
Helio: 下拉框类组件字段映射规则常量
 */

/**
 * TreeSelect 组件默认字段映射规则
 */
export const DEFAULT_TREE_SELECT_FIELD_NAMES = {
  // 组件 title 字段：对应后端返回体中的 title 字段
  title: 'title',
  // 组件 key 字段：对应后端返回体中的 id 字段
  key: 'id',
  // 组件 value 字段：对应后端返回体中的 id 字段
  value: 'id',
};

/**
 * Select 组件默认字段映射规则
 */
export const DEFAULT_SELECT_FIELD_NAMES = {
  // 组件 label 字段：对应后端返回体中的 title 字段
  label: 'title',
  // 组件 key 字段：对应后端返回体中的 id 字段
  key: 'id',
  // 组件 value 字段：对应后端返回体中的 id 字段
  value: 'id',
};
